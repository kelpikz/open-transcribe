{
  "name": "codex-audio",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": { "codex-audio": "./src/cli.ts" },
  "scripts": {
    "start": "bun run ./src/cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build --compile --outfile dist/codex-audio ./src/cli.ts"
  },
  "dependencies": {
    "werift": "^0.24.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
